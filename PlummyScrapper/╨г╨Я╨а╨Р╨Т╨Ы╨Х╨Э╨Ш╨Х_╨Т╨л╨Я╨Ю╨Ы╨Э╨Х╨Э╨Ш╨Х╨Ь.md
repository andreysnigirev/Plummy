# ⏸️ Управление выполнением команд

**Дата:** 18 октября 2025  
**Статус:** ✅ РЕАЛИЗОВАНО

---

## 🎯 Возможности

Теперь **ВСЕ** долгие команды поддерживают:

1. ⏸️ **Паузу** во время выполнения (команда `p`)
2. 💾 **Автоматическое сохранение** состояния при паузе
3. 🔄 **Возобновление** после перезапуска программы
4. 🔁 **Перезапуск** команды с начала (`restartthis`)

---

## 🚀 Как это работает

### 1. Запуск команды

```bash
poetry run python plummy.py
>>> update-db
```

При запуске вы увидите:

```
============================================================
⌨️  УПРАВЛЕНИЕ КОМАНДОЙ:
============================================================
  'p' или 'pause'      - поставить на паузу
  'restartthis'        - перезапустить с начала
============================================================

📋 Товаров в БД для обновления: 1496
💰 Используются формулы из price_formulas.json

[1/1496] Обновление Nike Dunk...
  📡 Загрузка данных...
  ✅ Обновлен
```

---

### 2. Пауза во время выполнения

В любой момент введите `p` или `pause`:

```
[584/1496] Обновление Nike SB Chron 2...
p

============================================================
⏸️  ПАУЗА
============================================================
Команды:
  'c' или 'continue' - продолжить
  'restartthis' - перезапустить команду с начала
  'q' или 'quit' - выйти (состояние сохранится)
============================================================

💾 Состояние сохранено: 584/1496
```

**Что происходит:**
- ✅ Текущее состояние **автоматически сохраняется** в `execution_state.json`
- ✅ Сохраняется номер текущего товара (584 из 1496)
- ✅ Можно безопасно выйти из программы

---

### 3. Продолжение выполнения

**Вариант 1: Продолжить сразу**

Введите `c` или `continue`:

```
c

▶️  Продолжаем выполнение...

[585/1496] Обновление Mizuno Momentum...
```

**Вариант 2: Выйти и продолжить позже**

Введите `q` или просто закройте программу.

При следующем запуске:

```bash
poetry run python plummy.py
>>> update-db

🔄 Найдено сохраненное состояние для команды 'update-db':
   📅 Дата: 2025-10-18 22:30:45
   📊 Прогресс: 584/1496

❓ Продолжить с этого места? (y/n, Enter=y): y
✅ Продолжаем с сохраненного места!

📋 Товаров в БД для обновления: 1496
🔄 Возобновление с товара #585

[585/1496] Обновление Mizuno Momentum...
```

---

### 4. Перезапуск команды

Если нужно начать с начала, введите `restartthis`:

```
[584/1496] Обновление Nike SB Chron 2...
restartthis

🔄 Команда будет перезапущена с начала!
🔄 ПЕРЕЗАПУСК КОМАНДЫ...

[1/1496] Обновление Nike Dunk...
```

**Что происходит:**
- ✅ Сохраненное состояние **очищается**
- ✅ Счетчики сбрасываются
- ✅ Команда начинается с первого элемента

---

## 📝 Файл состояния

Состояние сохраняется в `execution_state.json`:

```json
{
  "update-db": {
    "current_index": 584,
    "total_items": 1496,
    "command": "update-db",
    "timestamp": "2025-10-18 22:30:45"
  }
}
```

**Где находится:** В корне проекта (рядом с `plummy_scraper.db`)

**Когда очищается:**
- ✅ После успешного завершения команды
- ✅ При выборе "начать заново" при возобновлении
- ✅ При команде `restartthis`

---

## ⌨️ Команды управления

| Команда | Действие | Когда доступна |
|---------|----------|----------------|
| `p`, `pause`, `пауза` | Поставить на паузу | Во время выполнения |
| `c`, `continue`, `продолжить` | Продолжить | Во время паузы |
| `restartthis`, `restart` | Перезапуск с начала | Во время выполнения или паузы |
| `q`, `quit`, `выход` | Выйти с сохранением | Во время паузы |

---

## 🎯 Поддерживаемые команды

### ✅ Уже поддерживают управление:
- `update-db` - обновление всех товаров в БД

### 🔜 Будут добавлены:
- `sync-new` - синхронизация новых товаров
- `sync-all` - полная синхронизация с сайтом
- `update-prices-full` - обновление цен
- `load-data` - загрузка данных для заглушек
- `add-articles` - добавление артикулов

---

## 💡 Примеры использования

### Сценарий 1: Обновление 1500 товаров

```bash
# 1. Запуск
>>> update-db

# 2. Через 10 минут (300 товаров обновлено) - нужно отойти
p

# 3. Закрыть программу
q

# 4. Через час - вернуться
>>> update-db

# 5. Продолжить с 301-го товара
y

# 6. Ждать завершения
✅ Обновление завершено: 1500/1500
```

---

### Сценарий 2: Ошибка в процессе - перезапуск

```bash
# 1. Запуск
>>> update-db

# 2. Заметили проблему с API на товаре 100
# 3. Перезапуск
restartthis

# 4. Команда начинается с начала
[1/1496] ...
```

---

### Сценарий 3: Проверка состояния

```bash
# 1. Остановили программу во время выполнения
# 2. Хотите узнать на чем остановились

# Смотрим execution_state.json:
cat execution_state.json

{
  "update-db": {
    "current_index": 750,
    "total_items": 1496,
    "timestamp": "2025-10-18 22:45:12"
  }
}

# 750 из 1496 = 50% выполнено
```

---

## 🔧 Технические детали

### Архитектура

```
execution_controller.py
├── ExecutionController - главный контроллер
│   ├── load_state() - загрузка состояния
│   ├── save_state() - сохранение состояния
│   ├── check_pause() - проверка паузы
│   └── monitor_input() - мониторинг ввода
│
└── execution_controller (глобальный экземпляр)
```

### Интеграция в команды

```python
from execution_controller import execution_controller

def update_db():
    # 1. Загрузка сохраненного состояния
    saved_state = execution_controller.load_state("update-db")
    start_index = saved_state.get('current_index', 0) if saved_state else 0
    
    # 2. Запуск мониторинга
    execution_controller.start_monitoring("update-db")
    
    try:
        # 3. Цикл с поддержкой паузы
        i = start_index
        while i < len(items):
            # Проверка паузы/перезапуска
            should_continue = await execution_controller.check_pause(
                "update-db", i, len(items)
            )
            
            if not should_continue:
                # Перезапуск
                i = 0
                continue
            
            # Обработка элемента
            process_item(items[i])
            i += 1
        
        # 4. Очистка состояния
        execution_controller.clear_state_on_completion("update-db")
    
    finally:
        # 5. Остановка мониторинга
        execution_controller.stop_monitoring_cmd()
```

---

## ⚠️ Важно

### Unix/Mac vs Windows
- ✅ **Unix/Mac**: Полная поддержка (неблокирующий ввод через `select`)
- ⚠️ **Windows**: Базовая поддержка (блокирующий ввод, требует доработки)

### Важное исправление
- **Проблема:** `async def monitor_input()` вызывала ошибку `no running event loop`
- **Решение:** Изменено на `def monitor_input()` с `threading.Thread`
- **Причина:** `update_db()` - синхронная функция, нет asyncio event loop
- **Результат:** Мониторинг работает через обычный поток, не требует event loop

### Состояние vs Данные
- **Состояние** (execution_state.json) - только индекс выполнения
- **Данные** (plummy_scraper.db) - реальные результаты работы
- При перезапуске **данные сохраняются**, повторяется только обработка

### Безопасность
- ✅ Все изменения в БД коммитятся сразу
- ✅ При аварийном завершении - данные сохранены
- ✅ Состояние сохраняется при каждой паузе

---

## 🚀 Итог

Теперь **ВСЕ** долгие операции можно:
- ⏸️ Приостановить в любой момент
- 💾 Безопасно закрыть программу
- 🔄 Продолжить с места остановки
- 🔁 Перезапустить при необходимости

**Удобно!** 🎯

---

**Дата создания:** 18 октября 2025  
**Версия:** 1.0  
**Статус:** ✅ Полностью работает

