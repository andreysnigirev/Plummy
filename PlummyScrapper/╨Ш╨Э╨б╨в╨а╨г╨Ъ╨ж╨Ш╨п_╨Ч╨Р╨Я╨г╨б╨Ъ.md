# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ

## ⚠️ КРИТИЧЕСКИ ВАЖНО!

### 📋 **ПРОБЛЕМА:**

Вы изменили формулы цен, но цены у товаров не обновились.

### 🔍 **ПРИЧИНА:**

У **ВСЕХ 11,963 вариантов** в БД `sku_id = NULL`

Команда `update-prices-full` не может обновить цены без `sku_id`, потому что не может сопоставить варианты из API с вариантами в БД.

---

## ✅ РЕШЕНИЕ

### Шаг 1: Запустите update-db

```bash
poetry run python plummy.py
```

В интерактивной оболочке введите:
```bash
>>> update-db
```

### Что это сделает:

1. ✅ Обновит данные всех товаров из Dewu API
2. ✅ Заполнит `sku_id` для каждого варианта
3. ✅ **Пересчитает цены по АКТУАЛЬНЫМ формулам**
4. ✅ Синхронизирует с сайтом

**Время:** ~50-60 минут (~1,500 товаров)

---

## 📊 ПОСЛЕ ВЫПОЛНЕНИЯ

### Шаг 2: Проверьте результат

1. **Проверьте товары на сайте** - цены должны обновиться
2. **Проверьте формулы** - они должны применяться корректно

### Шаг 3: Используйте update-prices-full

После того, как `sku_id` заполнен, команда `update-prices-full` будет работать корректно:

```bash
>>> update-prices-full
```

Эта команда будет **быстро** обновлять цены при изменении формул (без полного обновления товаров).

---

## 🎯 ПОЧЕМУ ТАК ПРОИЗОШЛО

1. **Поле `sku_id` было добавлено недавно** (18.10.2025)
2. **Старые данные** в БД были созданы без этого поля
3. **Команда `update-prices-full`** требует `sku_id` для работы
4. **Без `sku_id`** невозможно сопоставить варианты

---

## 📝 ТЕХНИЧЕСКАЯ ИНФОРМАЦИЯ

### Структура ProductVariant:

```
- id
- product_id
- sku_id          ← ЭТО ПОЛЕ БЫЛО ДОБАВЛЕНО
- size_eu
- size_type
- price_cny
- price_rub
- is_available
- stock_status
```

### Проверка в коде:

```python
if not variants_with_sku and product.variants:
    logger.warning("Товар имеет варианты без sku_id")
    return {'updated': 0, 'added': 0, 'removed': 0}
```

Теперь команда корректно обрабатывает ситуацию, когда `sku_id` отсутствует.

---

## ⚡ ДЕЙСТВУЙТЕ ПРЯМО СЕЙЧАС!

```bash
poetry run python plummy.py
>>> update-db
```

**Это единственный способ обновить цены!**

---

**Дата:** 18 октября 2025  
**Статус:** ✅ Готово к выполнению

